[package]
name = "zeromem-py"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Python bindings for zeromem"

[lib]
name = "zeromem"
crate-type = ["cdylib"]

[features]
default = ["fastembed"]
fastembed = ["zm/fastembed"]

[dependencies]
zm = { path = "../zeromem", package = "zeromem", default-features = false }
pyo3 = { version = "0.22", features = ["abi3-py39", "extension-module"] }
serde = "1"
serde_json = "1"
